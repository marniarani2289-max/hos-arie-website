import Link from "next/link";
import { registerAccount } from "../actions";
export const metadata={title:"Create Cyber Law Student Account | LexNusa",robots:{index:false,follow:false}};
export default async function Page({searchParams}:{searchParams:Promise<{error?:string;success?:string}>}){
 const q=await searchParams;
 return <main className="mx-auto max-w-xl space-y-6 px-6 py-14"><Link href="/lexnusa/mayantara" className="underline">Course guide</Link><h1 className="text-3xl font-bold">Create your student account</h1><p>Use your existing website account if you already have one. After signing in, request membership with your NIM; your lecturer approves classroom access.</p>{q.error&&<p role="alert">Registration could not be completed. Check your details or use your existing account.</p>}{q.success&&<p role="status">Check your email to confirm your account, then sign in below.</p>}<form action={registerAccount} className="space-y-5"><label className="block">Full name<input name="name" required minLength={2} maxLength={120} autoComplete="name" className="mt-2 w-full rounded border p-3"/></label><label className="block">Email<input name="email" type="email" required maxLength={254} autoComplete="email" className="mt-2 w-full rounded border p-3"/></label><label className="block">Password<input name="password" type="password" required minLength={8} autoComplete="new-password" className="mt-2 w-full rounded border p-3"/></label><button className="rounded-lg bg-teal-800 px-5 py-3 font-bold text-white">Create account</button></form><Link href="/login?next=%2Flexnusa%2Fmayantara%2Fworkspace" className="block text-teal-800 underline">Already have an account? Sign in</Link></main>;
}
