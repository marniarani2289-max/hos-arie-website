import Link from "next/link";
import { registerAccount } from "../actions";
export const metadata={title:"Buat Akun Mahasiswa Hukum Kejahatan Mayantara | LexNusa",robots:{index:false,follow:false}};
export default async function Page({searchParams}:{searchParams:Promise<{error?:string;success?:string}>}){
 const q=await searchParams;
 return <main className="mx-auto max-w-xl space-y-6 px-6 py-14"><Link href="/lexnusa/mayantara" className="underline">Panduan kuliah</Link><h1 className="text-3xl font-bold">Buat akun mahasiswa</h1><p>Gunakan akun situs yang sudah Anda miliki. Setelah masuk, ajukan keanggotaan dengan NIM; dosen akan menyetujui akses kelas.</p>{q.error&&<p role="alert">Pendaftaran tidak dapat diselesaikan. Periksa data Anda atau gunakan akun yang sudah ada.</p>}{q.success&&<p role="status">Periksa surel untuk mengonfirmasi akun, lalu masuk melalui tautan di bawah.</p>}<form action={registerAccount} className="space-y-5"><label className="block">Nama lengkap<input name="name" required minLength={2} maxLength={120} autoComplete="name" className="mt-2 w-full rounded border p-3"/></label><label className="block">Surel<input name="email" type="email" required maxLength={254} autoComplete="email" className="mt-2 w-full rounded border p-3"/></label><label className="block">Kata sandi<input name="password" type="password" required minLength={8} autoComplete="new-password" className="mt-2 w-full rounded border p-3"/></label><button className="rounded-lg bg-teal-800 px-5 py-3 font-bold text-white">Buat akun</button></form><Link href="/login?next=%2Flexnusa%2Fmayantara%2Fworkspace" className="block text-teal-800 underline">Sudah punya akun? Masuk</Link></main>;
}
