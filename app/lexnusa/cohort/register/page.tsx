import Link from 'next/link';
import {register} from './actions';
export const metadata={title:'Daftar | LexNusa Cohort'};
export default async function Page({searchParams}:{searchParams:Promise<{error?:string;success?:string}>}){
 const q=await searchParams;
 return <main className="mx-auto max-w-xl px-6 py-16"><Link className="text-teal-800 underline" href="/lexnusa/cohort">LexNusa Cohort</Link><h1 className="mt-8 text-4xl font-bold">Buat akun belajar</h1><p className="mt-4">Simpan proses menyusun kontrak, hasil AI, dan umpan balik fasilitator dalam ruang praktik pribadi.</p>{q.error&&<p role="alert" className="mt-5 bg-red-50 p-4">Pendaftaran belum berhasil. Periksa isian atau gunakan akun yang sudah terdaftar.</p>}{q.success&&<p role="status" className="mt-5 bg-teal-50 p-4">Periksa email untuk konfirmasi akun, kemudian masuk ke kelas.</p>}<form action={register} className="mt-8 grid gap-5"><label>Nama lengkap<input required name="name" minLength={2} maxLength={120} autoComplete="name" className="mt-2 w-full rounded-lg border p-3"/></label><label>Email<input required name="email" type="email" autoComplete="email" className="mt-2 w-full rounded-lg border p-3"/></label><label>Kata sandi (minimal 8 karakter)<input required name="password" type="password" minLength={8} autoComplete="new-password" className="mt-2 w-full rounded-lg border p-3"/></label><button className="rounded-lg bg-teal-800 p-4 font-bold text-white">Daftar akun LexNusa</button></form><p className="mt-6">Sudah memiliki akun? <Link href="/login?next=%2Flexnusa%2Fcohort%2Fworkspace" className="text-teal-800 underline">Masuk kelas</Link></p></main>;
}
